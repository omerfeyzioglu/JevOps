# Optional live-provider credentials. Keep actual values in your environment or .env.
TYPESAFE_API_KEY=
# Offline benchmark comparators.
OPENAI_API_KEY=
GEMINI_API_KEY=
# Existing live-demo LLM comparator; not used by `jevops benchmark`.
ANTHROPIC_API_KEY=
# Run providers every Nth evidence snapshot (snapshots arrive once per event tick).
DECISION_EVERY_N_SNAPSHOTS=5
