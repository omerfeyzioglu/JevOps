# Optional provider credentials. Keep actual values in your environment or .env.
TYPESAFE_API_KEY=
OPENAI_API_KEY=
GEMINI_API_KEY=
# Run providers every Nth evidence snapshot (snapshots arrive once per event tick).
DECISION_EVERY_N_SNAPSHOTS=5
